name: your_project_name
description: A new Flutter project.

environment:
  sdk: '^3.4.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/images/ไม่มีพื้นหลัง (1).png
